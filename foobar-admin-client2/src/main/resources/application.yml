server:
  port: 8082

spring:
  application:
    name: admin-client
  config:
    import:
      - classpath:spring-boot-admin-client.yml
      - classpath:actuator.yml
logging:
  level:
    root: debug